# 保存体験の改善

ChatGPT用「まとめて記録」の保存状態を明確にしました。

- 端末保存済み、送信待ち、同期中、同期失敗、クラウド同期済みを区別します。
- 未送信データを利用者ごとに端末へ保存し、再読み込み後に復元します。
- 送信中の更新を未送信のまま保持します。
- 同期が入力途中のフォームを書き換えないようにしました。
- まとめて記録画面から同期を再試行できます。
- 別端末の新しいデータに更新を拒否された場合、送信済みにせず競合を表示します。

## 確認

`node tests/save-reliability.cjs`

通信失敗、未送信データの復元、送信中の更新、送信完了、競合、容量不足、オフライン表示を模擬して確認しています。本番Supabaseと実機ブラウザーでの結合確認は未実施です。

## 反映

既存の公開先の app.js と index.html を更新してください。データベース変更はありません。GitHub Pagesの公開ブランチに反映すると、通常の公開処理で配信されます。

## 制約

同じ日の複数端末編集を項目単位で自動統合する変更は含んでいません。クラウドが新しい場合は競合を表示します。ブラウザーのデータ削除や端末紛失に備えるにはクラウド同期の完了が必要です。未送信データの復元には同じ利用者でのログインが必要です。

